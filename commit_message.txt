fix: provide HttpClient in NetworkModule for Hilt Worker

- Resolves CI failure: `io.ktor.client.HttpClient cannot be provided without an @Inject constructor or an @Provides-annotated method`.
- Fixes `NetworkModule` to provide `HttpClient(OkHttp)` so `DriveSyncRepository` injected via `WorkspaceSyncWorker` can be instantiated by Hilt.
- Removes duplicate `InitializationProvider` overrides in `AndroidManifest.xml`.
- Adds Ktor ContentNegotiation/Json dependencies to `build.gradle.kts`.
